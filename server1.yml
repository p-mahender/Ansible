---
- hosts: DBservers
  become: yes
  tasks:
  - name: install websever on ubuntu ansible nodes
    yum:
      name: "{{mywebpackforubuntu}}"
      state: present
    when: ansible_os_family=="RedHat"